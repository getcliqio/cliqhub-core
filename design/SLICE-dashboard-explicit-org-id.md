# SLICE: Dashboard explicit `org_id` + drop header tenancy (DASH-ORG)

**Status:** implemented on branch `slice/dashboard-explicit-org-id` — **not** merged to `main`

## Checklist

- [x] Core Zod + assert + unit
- [x] SPA home dashboard
- [x] OpenAPI (internal) + suites + push
