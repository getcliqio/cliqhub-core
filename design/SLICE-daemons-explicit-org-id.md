# SLICE: Daemons explicit `org_id` + drop header tenancy (DAE-ORG)

**Status:** implemented on branch `slice/daemons-explicit-org-id` — **not** merged to `main`

## Checklist

- [x] Core schema + controller + unit test
- [x] SPA admin + hub_activity
- [x] OpenAPI + suites + push
